{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "name": "Launch",
            "preLaunchTask": "npm: build",
            "console": "externalTerminal",
            "request": "launch",
            "program": "${workspaceFolder}/src/index.ts",
            "outFiles": [
                "${workspaceFolder}/**/*.js"
            ],
            "cwd": "${workspaceFolder}"
        }
    ]
}